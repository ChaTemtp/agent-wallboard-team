const { AGENT_STATUS, VALID_STATUS_TRANSITIONS } = require('./utils/constants');

console.log("All statuses:", AGENT_STATUS);
console.log("Available transitions:", VALID_STATUS_TRANSITIONS.Available);
